#Run Source (Ctrl+Shift+S) and Run Selected Line(Ctrl+Enter)
#Command + Enter で実行？

#計算
#和+ 差- 積* 商/ 冪乗^ 整数商%/% 剰余%%
#値
#ナル「何も示さない」NULL 欠損地(Not Available) NA 非数値(Not a Number) NaN 無限大(Infinity) Inf
#正弦 sin() 余弦 cos() 正接 tan() ルートsqrt()
#文字列"" 文字列の結合 paste("","",sep="") 文字列として認識 as.character()
#交差1の数列 (number):(number) または seq(,) cずつ追加 by=c c等分 length=c
#リピート rep() n回数 times=n n個 length=n
#ベクトル関数
#総和 sum() 不偏微分 var() 総積 prod 累積和昇順整列 sort 要素逆順 rev() 並列最大値 pmax 並列最小値 pmin()
#マッチング match() 前進差分 dif() 並列した各要素の順位 rank() 整列した各要素の元の位置 order()
#分析
#平均 mean() 平均(因子) ave() 中央値 median() 相関係数 cor() 標準偏差 sd() 最大値 max() 最小値 min() 範囲 range() 部位 quantile() 四分位偏差 IQR() 
#データフレームの列ごとの特徴 summary() 簡素なオブジェクト情報 str()
#データフレームの作成 (列名1=ベクトル1,列名2=ベクトル2,....) data.frame() リスト作成 list() グループ化 levels() カテゴリーを要素化したベクトル作成 factor()
#ベクトルの行列変換 (ベクトル,行列,列数) もしくは (ベクトル,nrow=,ncol=) matrix() 縦に並べて結合 rbind()
#横に並べて結合 cbind() dim属性の確認 dim() 行数の確認 nrow() 列数の確認 ncol()
#グラフ 
#ヒストグラム hist() 棒グラフ barplot() 散布図 plot
#ベクトル演算(x,y)
#和集合 union(x,y) 積集合 intersect(x,y) 差集合 setdiff(x,y) 集合として等しいか否か setequal(x,y) x中の各要素は集合yに含まれるか否か is.element(x,y) もしくは x %in% y
